<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/segments_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent">
  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/margin_base"
    android:paddingBottom="@dimen/margin_base">
    <TextView
      android:id="@+id/segments_empty"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:paddingStart="@dimen/margin_base"
      android:paddingEnd="@dimen/margin_base"
      android:text="@string/cycling_segments_empty"
      android:textAppearance="@style/MwmTextAppearance.Body4"
      android:visibility="gone" />
    <LinearLayout
      android:id="@+id/segments_list"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="vertical" />

    <!-- Shown even with no segments: a rider's first one often arrives from a friend, which is
         exactly when the list is empty. -->
    <Button
      android:id="@+id/segments_import"
      style="@style/CyclingMediaAppButton"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginStart="@dimen/margin_base"
      android:layout_marginTop="@dimen/margin_base"
      android:text="@string/cycling_segment_import" />
  </LinearLayout>
</ScrollView>