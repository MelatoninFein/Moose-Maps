<?xml version="1.0" encoding="utf-8"?>
<!-- One row in the sensor list: paired sensors show state and battery, discovered ones show RSSI. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:background="?selectableItemBackground"
  android:gravity="center_vertical"
  android:minHeight="@dimen/height_item_oneline"
  android:orientation="horizontal"
  android:paddingStart="@dimen/margin_base"
  android:paddingEnd="@dimen/margin_base">

  <ImageView
    android:id="@+id/sensor_icon"
    android:layout_width="@dimen/cycling_touch_target"
    android:layout_height="@dimen/cycling_touch_target"
    android:padding="@dimen/cycling_touch_padding"
    android:layout_marginEnd="@dimen/margin_base"
    android:contentDescription="@string/cycling_sensors_title"
    app:srcCompat="@drawable/ic_cycling_sensor"
    app:tint="?iconTint" />

  <LinearLayout
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:orientation="vertical"
    android:paddingTop="@dimen/margin_half"
    android:paddingBottom="@dimen/margin_half">

    <TextView
      android:id="@+id/sensor_name"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:ellipsize="end"
      android:lines="1"
      android:textAppearance="@style/MwmTextAppearance.Body1"
      tools:text="CYCPLUS C3" />

    <TextView
      android:id="@+id/sensor_status"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:ellipsize="end"
      android:lines="1"
      android:textAppearance="@style/MwmTextAppearance.Body4"
      tools:text="Connected · Speed, Cadence · 82%" />
  </LinearLayout>

  <ImageView
    android:id="@+id/sensor_action"
    android:layout_width="@dimen/cycling_touch_target"
    android:layout_height="@dimen/cycling_touch_target"
    android:padding="@dimen/cycling_touch_padding"
    android:background="?selectableItemBackgroundBorderless"
    android:contentDescription="@string/cycling_sensors_forget"
    app:srcCompat="@drawable/ic_close"
    app:tint="?iconTint" />
</LinearLayout>
